# Database
# Format: postgresql://username:password@host:port/database
# For local development with PostgreSQL installed via Homebrew:
DATABASE_URL="postgresql://your_username@localhost:5432/quiz_platform"

# JWT Authentication
# Generate a strong secret key for production (e.g., using: openssl rand -base64 32)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL="http://localhost:3000"
